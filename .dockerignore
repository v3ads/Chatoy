# Keep the build context small and the backend image clean.
.git
.gitignore
.venv
__pycache__
*.pyc
.pytest_cache
.env
.env.*

# Frontend is built/deployed separately (Vercel).
web

# Dev-only
tests
*.md
